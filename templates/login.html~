{% extends "base.html" %}

{% block tab-title %}Stack Underflow - LogIn{% endblock %}

{% block page-title %}Log In{% endblock %}


{% block page-content %}
	{% if form.errors %}
	<div class="red">
		{% for error in form.non_field_errors %}
            {{ error }}
        {% endfor %}
	</div>
	{% endif %}
    <div class="row">
        <form name="login" method="post">
	        {% for field in form %}
                <div class="col-sm-2 margin-tb5">
                   {{ field.label }}
                </div>
                <div class="col-sm-10 margin-tb5">
				    {{ field }}
			    </div>
            {% endfor %}
            <div class="col-sm-2 margin-tb5"></div>
		    <div class="col-sm-10 margin-tb5">
			    <button type="submit" class="btn btn-default">Log In!</button> 
		    </div>
        </form>
    </div>
{% endblock %}
